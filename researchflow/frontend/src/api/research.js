import axios from 'axios';
import { API_BASE_URL } from '../utils/constants';

export const createResearch = async (topic) => {
    // return axios.post(`${API_BASE_URL}/api/research`, { topic });
};
